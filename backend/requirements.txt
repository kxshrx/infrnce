# Core FastAPI dependencies
fastapi
uvicorn[standard]
python-dotenv
pydantic

# Data processing (still needed for other parts of your code)
pandas
numpy

# HTTP client for API calls
requests

# LLM integration via LangChain
langchain
langchain-groq

gradio_client
